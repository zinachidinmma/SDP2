const {
  room_numbers,
  room_types,
  work_status,
  dorms,
  categories,
} = require("../constant");

const permittedConstants = {
  room_numbers,
  room_types,
  work_status,
  dorms,
  categories,
};

module.exports = permittedConstants;
