const { room_numbers, room_types, work_status, dorms } = require("../constant");

const permittedConstants = {
  room_numbers,
  room_types,
  work_status,
  dorms,
};

module.exports = permittedConstants;
